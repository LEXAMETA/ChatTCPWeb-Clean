
{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "echo 'Installing dependencies' && npm install --no-cache --verbose",
        "buildCommand": "echo 'Node version:' && node -v && echo 'Running build' && npm run vercel-build && npm run postbuild",
        "outputDirectory": "web-build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
